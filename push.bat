rem git add -u = looks at all the already tracked files and stages the changes to those files if they are different or if they have been removed. It does not add any new files.
git add -u

rem git add . = add all new files

git add .


git commit -m "new pushed version"

git push https://github.com/karstenheld3/karstenheld.de.git master
pause